name = "courts"
main = "src/index.ts"
compatibility_date = "2024-09-01"

[[d1_databases]]
binding = "DB"
database_name = "courtshare"
database_id = "local-dev"   # Replace with real ID after: wrangler d1 create courtshare

# Secrets (set via: wrangler secret put APP_SECRET)
# APP_SECRET  — 32+ random chars, used to encrypt stored credentials
